#include <iostream>

using namespace std;

int main(void) {
    cin.tie(NULL);
    cin.sync_with_stdio(false);

    int M, V, n, K;
    cin >> M >> V >> n >> K;
    cout << (2 * n) + 40 <<'\n';

    return 0;
}